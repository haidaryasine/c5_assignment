# Core MCP server framework
mcp[fastmcp]>=1.2.0

# OpenAI Agents SDK
openai>=1.57.0

# HTTP client for calling external map APIs (Nominatim, OSRM)
httpx>=0.27.0

# Pydantic for tool schemas (comes with MCP but we pin for safety)
pydantic>=2.7.0

# Async server runtime (FastAPI/Starlette dependencies are bundled in FastMCP)
uvicorn>=0.29.0

# Optional: testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
